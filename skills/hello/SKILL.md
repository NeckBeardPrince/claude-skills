---
description: Smoke-test skill that confirms the adam-skills plugin loaded correctly. Invoke manually with /adam-skills:hello.
disable-model-invocation: true
---

Reply with a one-line confirmation that the `adam-skills` plugin is installed and loaded, including the plugin version from `plugin.json` if visible in context. Keep it dry.
